// This is a placeholder for email configuration
// In a production app, you would use a service like SendGrid, Mailgun, etc.
export default {
  from: process.env.EMAIL_FROM || 'noreply@mavo.com',
  // Add other email configuration as needed
};
